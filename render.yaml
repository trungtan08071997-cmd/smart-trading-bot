FROM python:3.12.10

WORKDIR /app
COPY . .

RUN pip install --upgrade pip
RUN pip install -r requirements.txt

# Giúp bot tự chạy lại nếu gặp lỗi
CMD ["bash", "-c", "while true; do python ai_optimizer_bot.py; sleep 5; done"]
